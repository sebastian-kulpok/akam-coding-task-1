In order to compile TypeScript files and recompile them on any changes, run the following command:
```
npm run build-and-watch
```

In order to execute tests, run the following command:
```
npm test
```